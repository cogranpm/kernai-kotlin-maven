package com.parinherm.entity.schema

import org.jetbrains.exposed.dao.id.LongIdTable
import org.jetbrains.exposed.sql.`java-time`.date

object {{#capitalize entityDef.name}}{{/capitalize}}s: LongIdTable() {
    {{#each fieldDefinitions}}val {{name}} = {{{schemaDeclaration}}}
    {{/each}}
    {{#each (getForeignKeysByView this)}} val {{this.entityDef.name}}Id = long("{{this.entityDef.name}}Id").references({{#capitalize this.entityDef.name}}{{/capitalize}}s.id)
    {{/each}}
}